import { CSSProperties } from "react";

const StepperStyle: CSSProperties = {
  background: "#00684a",
  padding: "3rem 0rem",
  width: "100%",
  marginBottom: "0.3rem",
};

const StepperDivStyle: CSSProperties = {
  color: "white",
};

const styles = {
  StepperStyle,
  StepperDivStyle,
};

export default styles;
