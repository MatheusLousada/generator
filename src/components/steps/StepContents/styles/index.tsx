import { CSSProperties } from "react";

const ActiveStepContentStyle: CSSProperties = {
  width: "100%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};

const styles = {
  ActiveStepContentStyle,
};

export default styles;
